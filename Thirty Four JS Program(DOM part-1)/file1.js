//DOM(Document Object Model)

//to display in normal html format
console.log(window.document);   //inside window object browser add 
                                // document object which is treated as property of window object

//to display in Javascript format
console.dir(document);  //by default window object is present there always 